import { createContext } from 'react'

/**
 * Участвуем ли мы сейчас в чужой общей сессии.
 *
 * Признак нужен строкам списков — там появляется кнопка «в общую очередь», —
 * а строк на экране бывают сотни, и они виртуализованы. Пробрасывать его через
 * четыре экрана и список значило бы переписать пять наборов свойств ради
 * одного булева значения, а подписывать каждую строку на состояние плеера —
 * платить за это на каждой прокрутке. Контекст обходится одним объявлением
 * наверху и одним чтением внизу.
 */
export const JamGuestContext = createContext(false)
